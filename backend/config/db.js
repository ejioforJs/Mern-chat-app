// const mongoose = require('mongoose')

// const connectdb = async() => {
//     try {
//         const conn = await mongoose.connect(process.env.MONGODB_URI).then(() => console.log("connected to db"))
//     } catch (error) {
//         console.log(`error: ${error.message}`)
//         process.exit()
//     }
// }

// module.exports = connectdb